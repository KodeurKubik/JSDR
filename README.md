# JSDR - JavaScript Object Run
A module that add a way to "run" a json file and add so much posibilities with the .json files. All the code you can put in the json file is writen on this GitHub reposit.
